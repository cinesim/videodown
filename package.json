{
  "name": "videodown-monorepo",
  "version": "0.20.0-dev",
  "description": "videodown monorepo root",
  "private": true,
  "license": "MIT",
  "homepage": "https://github.com/cinesim/videodown",
  "packageManager": "bun@1.3.13",
  "engines": {
    "node": ">=20.19.0",
    "bun": ">=1.3.0"
  },
  "workspaces": [
    "packages/*",
    "packages/muya/examples",
    "packages/muya/e2e"
  ],
  "overrides": {
    "postcss": "8.5.15",
    "esbuild@>=0.27.0 <0.28.1": "0.28.1"
  },
  "trustedDependencies": [
    "ced",
    "electron",
    "electron-winstaller",
    "esbuild",
    "keytar",
    "native-keymap",
    "sharp",
    "unrs-resolver",
    "workerd"
  ],
  "scripts": {
    "postinstall": "tsx scripts/postinstall.ts",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "minify-locales": "tsx scripts/minify-locales.ts",
    "rebuild-native": "bun run --filter videodown rebuild-native",
    "start": "bun run --filter videodown start",
    "dev": "bun run --filter videodown dev",
    "build": "bun run --filter videodown build",
    "build:unpack": "bun run --filter videodown build:unpack",
    "build:win": "bun run --filter videodown build:win",
    "build:win:x64": "bun run --filter videodown build:win:x64",
    "build:win:arm64": "bun run --filter videodown build:win:arm64",
    "build:mac": "bun run --filter videodown build:mac",
    "build:mac:x64": "bun run --filter videodown build:mac:x64",
    "build:mac:arm64": "bun run --filter videodown build:mac:arm64",
    "build:linux": "bun run --filter videodown build:linux",
    "perf:inspect": "bun run --filter videodown perf:inspect",
    "perf:inspect-brk": "bun run --filter videodown perf:inspect-brk",
    "test": "bun run --filter videodown test",
    "test:unit": "bun run --filter videodown test:unit",
    "test:e2e": "bun run --filter videodown test:e2e",
    "typecheck": "bun run --filter videodown typecheck",
    "typecheck:watch": "bun run --filter videodown typecheck:watch",
    "check": "bun run format:check && bun run lint && bun run typecheck",
    "gen-third-party": "tsx scripts/generateThirdPartyLicense.ts",
    "validate-licenses": "tsx scripts/validateLicenses.ts",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{js,mjs,cjs,ts,tsx,vue}": [
      "oxlint --fix",
      "oxfmt --write"
    ],
    "**/*.{css,scss,less,html,json,jsonc,json5,md,mdx,yaml,yml,toml}": [
      "oxfmt --write"
    ]
  },
  "devDependencies": {
    "electron": "~42.1.0",
    "husky": "^9.1.7",
    "license-checker": "^25.0.1",
    "lint-staged": "^17.2.0",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "tsx": "^4.22.4"
  }
}
